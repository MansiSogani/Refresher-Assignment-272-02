//load professor name and timings of class when user hovers over the class code.

function mOver(obj) {
    obj.innerHTML = "By Simon Shim on Mon, Wed 4:30-5:45"
}

function mOut(obj) {
    obj.innerHTML = "CMPE 283-01"
}